# --- Core API ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4

# --- Async task pipeline ---
celery==5.4.0
redis==5.0.6

# --- Graph database driver ---
neo4j==5.21.0

# --- Phonetic aliasing (Double Metaphone) for alias/nickname resolution
# e.g. "Shaikh" / "Sheikh" / "Shekh" collapse to the same phonetic code ---
metaphone==0.6

# --- Utilities ---
python-dotenv==1.0.1

# --------------------------------------------------------------------------
# PRODUCTION / REAL-DEPLOYMENT NOTE:
# On an actual forensics workstation with GPU + pre-mirrored model weights,
# the AI layer in tasks.py would additionally depend on:
#   paddleocr==2.7.*         (scanned FIR / document OCR)
#   faster-whisper==1.0.*    (CDR call-recording / intercept transcription)
#   an ONNX/PyTorch IndicBERT-NER checkpoint served locally
# These are intentionally NOT installed in this prototype image to keep the
# container small and buildable entirely offline on a laptop; tasks.py
# mocks their outputs with deterministic stub functions so the full
# ingestion -> graph pipeline can be demonstrated end-to-end.
# --------------------------------------------------------------------------
